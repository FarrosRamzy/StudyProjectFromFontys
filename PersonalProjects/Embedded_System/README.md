# Archived Embedded Software Copy

This folder appears to contain a second copy of the PlatformIO embedded projects also stored under `EmbeddedSoftware_VSCode`.

To avoid maintaining duplicate documentation, the main project READMEs are located in:

- `EmbeddedSoftware_VSCode/I2Cbus_program`
- `EmbeddedSoftware_VSCode/rider_led`
- `EmbeddedSoftware_VSCode/traffic_light_system`

For a cleaner public repository, I would keep one copy and remove this duplicate after confirming that no unique source files are present.
